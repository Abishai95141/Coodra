# Live MCP demo — all 9 tools through Claude Code

Demonstrating the full MCP surface end-to-end after the user restarted Claude Code,
which spawned a fresh subprocess against the post-S17 dist (with all 6 verification fixes landed).

## Session shape
- runId: `run:proj_bd336220-...:stdio-174c17ad-...`  ← hyphen separator (post-S17)
- transport: stdio (Claude Code → apps/mcp-server/dist/index.js)
- mode: solo (CLERK_SECRET_KEY = sentinel → bypass)
- DB: SQLite at default path (~/.contextos/data.db) — auto-migrate at boot ran cleanly

## Tools exercised live

| Tool | Result |
|---|---|
| ping | round-trip ok, hyphen-separated sessionId |
| get_run_id | run minted, projects auto-created in solo mode |
| get_feature_pack(02-mcp-server) | full pack (158 KB) returned with parent inheritance from 01-foundation |
| record_decision × 2 | 2 distinct decisions persisted; retry deduped to original ID with created:false |
| check_policy(Write /tmp/demo.ts) | allow / no_rule_matched / failOpen:false; audit row dispatched asynchronously |
| query_codebase_graph(createDb) | codebase_graph_not_indexed soft-failure with howToFix |
| save_context_pack | this very pack |
| query_run_history | (called next — should show this run with title joined) |
| search_packs_nl | (called next — LIKE fallback with no_embeddings_yet) |

## What this proves
- Live Claude Code session can drive the full MCP surface
- Auto-migrate at boot works (Fix §8.1)
- sessionId no-colon constraint propagated to transport (Fix §8.6)
- Pack filename will be sanitized (Fix §8.4)
- Soft-failure canonical shape consistent across record_decision, check_policy, query_codebase_graph
- Idempotency dedupe behaves as locked
